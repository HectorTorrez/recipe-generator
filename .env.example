# Frontend: API worker URL (used at build time)
VITE_API_URL=http://localhost:8787

# API worker: comma-separated allowed CORS origins
# Set in api/wrangler.jsonc vars.ALLOWED_ORIGINS for production.
